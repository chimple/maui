import 'package:flutter/material.dart';

class UserCollection extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Container(
      child: new Text("User Collection"),
    );
  }
}